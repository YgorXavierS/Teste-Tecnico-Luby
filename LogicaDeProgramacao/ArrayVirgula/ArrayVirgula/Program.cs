using System;
using System.Collections;
namespace ArrayVirgula
{
    class Program
    {
        static void Main(string[] args)
        {
            int ancora = 0;
            string entrada = Console.ReadLine();
            string[] vet = entrada.Split(",");
            ArrayList trybet = new ArrayList();

            
           /*
            for (int i = 0; i < vet.Length; i++)

            {
                if (ancora < 2)
                {
                     trybet.Add(new[] {vet[i]});
                }
                else
                {
                    ancora = 0;
                }
              
            }


            //int[] teste = (int[])trybet.ToArray(typeof(int));
            Console.WriteLine(trybet.TrimToSize());
           
           */


        }
    }
}
