using System;
using System.Collections.Generic;
using System.Collections;
using System.Text;

namespace ArrayVirgula
{
    class ArreyBack

    {
           ///   public static string[] TransformarEmMatriz(string[] vetor){

            
            ///for(int x = 0; x < vetor.Length; x += 2)
            //{
               
            //}
        //}
        /*
        string let = "1789, 3, 8 , 1788, 3, 8, 1790, 3, 9";
        let array = new Array();
        let quebrado = texto.split(",");
            for (let i = 0; i<quebrado.length; i += 3) array.push([parseInt(quebrado[i]), parseInt(quebrado[i + 1]), parseInt(quebrado[i + 2])]);
         console.log(array);
        */
    }
}
